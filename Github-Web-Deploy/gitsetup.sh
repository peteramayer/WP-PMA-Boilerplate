pwd;
# if [ test -e ~/.ssh/id_rsa ];
# 	then
# 		echo "Key is already setup...";
# 		cat ~/.ssh/id_rsa.pub;
# 		fi;
# 	else 
# 		echo "Key does not exist. Setting Up Key...";
# 		#cd ~/.ssh;
# 		#ssh-keygen -t rsa -C "server@peteramayer.com" -N "" -f id_rsa;
# fi;
#git clone git@github.com:peteramayer/WP-PMA-Boilerplate.git #gitreponame
